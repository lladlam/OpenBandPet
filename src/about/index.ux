<template>
  <div class="page-container">
    <!-- Standardized Page Header -->
    <div class="page-header-container">
      <div class="page-header">
        <image src="../common/back.png" class="page-header-back-button" onclick="goBack"></image>
        <div class="header-title-time">
          <text class="page-time-display">{{ time }}</text>
          <text class="page-header-title">关于</text>
        </div>
      </div>
    </div>

    <!-- Page Specific Content -->
    <div class="page-content">
      <div class="content">
        <text class="app-name">BANDPET</text>
        <text class="app-version">版本 0.4.3</text>
        <text class="dev-title" style="margin-top: 30px;">参与开发的人员</text>
        <div class="dev-list">
          <text class="dev-name">@lladlam</text>
          <text class="dev-name">@Eric齐齐(0.4.0退)</text>
          <text class="dev-name">@御与魚余鱼</text>
          <text class="dev-name">@无源流沙</text>
        </div>
      </div>
    </div>
  </div>
</template>

<style>
  /* Standard Page Styles (copied from settings/index.ux) */
  .page-container {
    flex-direction: column;
    align-items: center;
    background-color: #000000;
    width: 100%;
    height: 100%;
  }
  .page-header-container {
    width: 100%;
    margin-bottom: 20px;
  }
  .page-time-display {
    color: #FFFFFF;
    font-size: 24px;
    margin-bottom: 2px;
  }
  .page-header {
    width: 100%;
    height: 90px;
    padding: 0 20px;
    flex-direction: row;
    align-items: center;
    justify-content: center; /* Center title block */
    position: relative; /* Anchor for back button */
  }
  .page-header-back-button {
    width: 80px;
    height: 80px;
    position: absolute;
    left: 0px;
  }
  .page-header-title {
    color: #FFFFFF;
    font-size: 32px;
  }
  .header-title-time {
    flex-direction: column;
    align-items: center;
  }
  .page-content {
    flex-direction: column;
    width: 100%;
    flex: 1;
    align-items: center;
    overflow-y: scroll;
  }

  /* Page-specific styles */
  .content {
    flex-direction: column;
    align-items: center;
    margin-top: 20px;
    width: 90%;
  }
  .app-name {
    color: #FFF;
    font-size: 45px;
    /* font-weight is not consistently supported, use font size for emphasis */
  }
  .app-version {
    color: #AAAAAA;
    font-size: 24px;
    margin-top: 10px;
  }
  .dev-title {
    color: #AAAAAA;
    font-size: 24px;
    margin-bottom: 20px;
  }
  .dev-list {
    flex-direction: column;
    align-items: center;
  }
  .dev-name {
    color: #FFFFFF;
    font-size: 28px;
    margin-bottom: 15px;
  }
</style>

<script>
  import router from '@system.router';
  export default {
    data: {
      time: '00:00'
    },
    onInit() {
      this.updateTime();
      setInterval(this.updateTime, 60000);
    },
    updateTime() {
      const now = new Date();
      const hours = now.getHours().toString().padStart(2, '0');
      const minutes = now.getMinutes().toString().padStart(2, '0');
      this.time = `${hours}:${minutes}`;
    },
    goBack() {
      router.back();
    }
  }
</script>
